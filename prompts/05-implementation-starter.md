Please read the following files/directories:
- `specs/05-implementation-starter.md`
- `backend`
- `../loopkind` (sibling review surface; historically called `sentry` in this spec)
- `quill-agent`

Implement per the spec: generate or extend starter code, types, routes, and app scaffolding as described. Keep scope aligned with the spec; do not turn the backend into a second brain—OpenClaw stays central. Prefer minimal, coherent changes over drive-by refactors.
